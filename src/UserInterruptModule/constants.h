// User Interrupt Constants

#pragma once

// #include <Arduino.h>

// User Interrupt
const byte PUSH_BUTTON = 2;
const byte DEBOUNCE_DELAY = 50;       // For pushbutton hardware (ms)
const int BUTTON_PUSH_INTERVAL = 500; // For detecting LONG_PUSH and DOUBLE_PUSH